version: '2'

services:
   openvpn:
     container_name: openvpn
     image: vip8/openvpn:latest
     environment:
       TZ: Asia/Shanghai
     volumes:
      - "./data:/etc/openvpn"
     ports:
      - '28039:1194/udp'
     cap_add:
      - NET_ADMIN
     restart: always
     privileged: true
